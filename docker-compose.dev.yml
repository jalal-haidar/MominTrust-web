version: '3.8'
services:
  web:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - ./:/app:delegated
      - /app/node_modules
    environment:
      - NODE_ENV=development
    ports:
      - "5173:5173"
    command: sh -c "npm ci --silent && npm run dev -- --host 0.0.0.0"
